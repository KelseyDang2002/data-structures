#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
this is using the separate chaining approach for the hash table

so we need a linked list data structure
linked list node struct for the items to be stored in the hash
*/ 

// node constructor

// hash table data structure

// hash table constructor

// hash function

/*
method to insert data into hash table
*/

/*
method to delete data from hash table
*/

/*
method to search for data in the hash table
*/

// main
int main(int argc, char* argv[]) {
    
    return 0;
}
